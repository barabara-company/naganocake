<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre), method: :patch do |f| %>
  <p>ジャンル名</p>
  <%= f.text_field :name, value: @genre.name, placeholder: "ジャンル名" %>
  <%= f.submit "変更を保存" %>
<% end %>