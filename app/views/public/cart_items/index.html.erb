<h1>カート</h1>

<% if @cart_items.empty? %>
  <p>カートには商品がありません。</p>
<% else %>
  <ul>
    <% @cart_items.each do |cart_item| %>
      <li>
        商品: <%= cart_item.item.name %> | 数量: <%= cart_item.amount %>
      </li>
    <% end %>
  </ul>
<% end %>